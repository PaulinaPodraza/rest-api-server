{
  "dependencies": {
    "cors": "^2.8.5",
    "express": "4.17.1",
    "express-router": "^0.0.1",
    "uuid": "^8.3.2"
  },
  "scripts": {
    "start": "node server.js",
    "build": "cd client && yarn install && yarn build"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
